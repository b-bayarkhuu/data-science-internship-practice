# Python / Jupyter / OS junk
__pycache__/
*.pyc
venv/
.venv/
.ipynb_checkpoints/
.DS_Store

# Note: the CSV datasets ARE committed on purpose — they are the practice data.

# Internal mentor guidance — never publish to the student-facing repo
CLAUDE.md
